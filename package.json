{
  "name": "batman-study",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf dist backend/node_modules infra/lambda.zip && cd backend && npm install --production && zip -r ../infra/lambda.zip handler.js package.json node_modules/ && cd ../infra && terraform init && terraform apply -auto-approve"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.5.0"
  }
}
